apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  labels:
    app: oauth2-proxy
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 4180
      targetPort: http
  selector:
    app: oauth2-proxy

